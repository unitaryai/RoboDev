# docker-compose.yaml — Local development environment for RoboDev.
#
# Runs the controller outside of Kubernetes using the "local" execution
# backend. Engine containers are launched dynamically by the controller
# and are not defined here.
#
# Usage:
#   make compose-up     # start the controller
#   make compose-down   # stop and remove containers

services:
  controller:
    build:
      context: .
      dockerfile: docker/controller/Dockerfile
    ports:
      - "8080:8080"   # metrics and health
      - "8081:8081"   # webhook receiver
    environment:
      - ROBODEV_CONFIG=/config/robodev-config.yaml
    volumes:
      - ./robodev-config.yaml:/config/robodev-config.yaml:ro
      - robodev-workspace:/workspace

volumes:
  robodev-workspace:
